# Lucy PoC — ONLY packages missing from RunPod base image
# torch, torchvision, flash-attn, xformers are already in the RunPod image — DO NOT reinstall

# Diffusion
diffusers>=0.35.1
omegaconf>=2.3.0
einops>=0.8.0
peft>=0.10.0

# Video
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
decord>=0.6.0
av>=12.0.0

# Profiling & analysis
tabulate>=0.9.0

# HuggingFace (usually already present)
sentencepiece>=0.2.0
